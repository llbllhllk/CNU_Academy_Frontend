import React from "react";
import ModalOverlay from "../components/ModalOverlay";

export default {
  title: "Component/ModalOverlay",
  component: ModalOverlay,
};

export const Default = (args) => {
  return <ModalOverlay {...args} />;
};
